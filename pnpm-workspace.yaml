# Allow these native dependencies to run their install/build scripts.
# (pnpm blocks build scripts by default; these binaries are required by Hydrogen/Oxygen.)
allowBuilds:
  esbuild: true
  unrs-resolver: true
  workerd: true
